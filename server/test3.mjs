import { makeProviders, makeStandardFetcher, targets } from '@movie-web/providers'; const providers = makeProviders({ fetcher: makeStandardFetcher(fetch), target: targets.ANY }); async function go() { console.log('Starting runAll...'); const o = await providers.runAll({ media: { type: 'movie', title: 'The Matrix', releaseYear: 1999, tmdbId: '603' } }); console.log('Done:', JSON.stringify(o, null, 2)); } go();
