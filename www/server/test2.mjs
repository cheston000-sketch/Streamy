import { makeProviders, makeStandardFetcher, targets } from '@movie-web/providers'; const providers = makeProviders({ fetcher: makeStandardFetcher(fetch), target: targets.ANY }); async function test() { try { console.log('Running 8stream...'); const stream = await providers.runSource({ media: { type: 'movie', title: 'Deadpool', releaseYear: 2024, tmdbId: '533535' }, sourceId: '8stream' }); console.log('Result:', stream); } catch(e) { console.error('Error:', e.message); } } test();
